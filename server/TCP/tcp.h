#pragma once
// ============================================================================
// TCPServer —— ScholarVPN TCP 传输模式（骨架，按 docs/TCP改造清单.txt 逐步实现）
// 目标：在 UDP 之外提供 TCP 承载，协议层（三阶段认证/加密/心跳报文）零改动，
//       仅传输层从数据报换成流式（每条消息 = tunnel_header(12B) + payload 分帧）
// 状态：空文件，待实现
// ============================================================================
